<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
          "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.kos.tr.old.login.dao.TerraLoginDAO">

<select id="terraLoginCheck" parameterType="terramembervo" resultType="terramembervo">
	SELECT 	
			A.MNUM		MNUM
	       ,A.MID 		MID
	       ,A.MPW 		MPW
	FROM 	TR_MEMBER A
	WHERE 	A.DELETEYN = 'Y'
	AND    A.MNUM = #{mnum}  	
	AND    A.MID = #{mid} 
	AND    A.MPW = #{mpw}	
</select>

</mapper>


